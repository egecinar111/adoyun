import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.flappyads',
  appName: 'Flappy Ads',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
